# Claude Instructions

This is the JPA project workspace.

Use this directory as the working directory. Use AIGST only as an external engine through AISkills MCP and the local router skill. Do not edit AIGST, AISkills, or GSD unless explicitly asked.

Before domain work, use AISkills MCP to select the relevant live skill. Read only the selected skill and load skill-local references only when needed.

For project files, keep edits inside this project unless the user explicitly asks to modify AIGST.
